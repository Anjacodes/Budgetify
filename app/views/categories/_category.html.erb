<%= link_to expenses_path(category.id) do %>
<div id="<%= dom_id category %>">
  <p>
    <strong>Name:</strong>
    <%= category.name %>
  </p>

  <p>
    <img class="w-1/6" src="<%= category.icon %>">
  </p>

  <p>
    <strong>Total Expenses:</strong>
    $<%= category.expenses.reduce(0) {|sum, num| sum + num.amount} %>
  </p>

</div>
<% end %>

